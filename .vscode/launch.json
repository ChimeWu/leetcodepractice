{
    // 使用 IntelliSense 了解相关属性。 
    // 悬停以查看现有属性的描述。
    // 欲了解更多信息，请访问: https://go.microsoft.com/fwlink/?linkid=830387
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Rust",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/target/debug/leetcodepractice", // 替换为你的程序名
            "args": [], // 如果你的程序需要命令行参数，可以在这里添加
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "cargo build", // 在启动前运行 "cargo build"
            // Remove the "sourceLanguages" property
        }        

    ]
}